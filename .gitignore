node_modules/
.wrangler/
dist/
.dev.vars
.env
.env.local
*.log
.DS_Store

# Worker / D1 local state
.mf/
.miniflare/

# Generated seed SQL (large; produced by scripts/seed-from-shards.ts)
migrations/seed/

# Local SSA data drops (used by scripts/local-ingest.ts)
data_src/
names.zip

# Enrichment pipeline: curated inputs in data/manual/ are tracked; the
# generated SQL (data/dist) and downloaded SSA zips (data/raw) are not.
data/dist/
data/raw/

# Legacy data dirs (pre-Cloudflare; kept ignored for any local checkout)
flat/
extra/states/
scripts/cache/

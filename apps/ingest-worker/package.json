{
  "name": "@nv/ingest-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test:state-ingest": "tsx src/states-ingest.test.ts && tsx src/upsert.test.ts && tsx src/diaspora-compute.test.ts",
    "test:scheduled": "wrangler dev --test-scheduled",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nv/shared": "*",
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260510.1",
    "typescript": "^5.5.4",
    "wrangler": "^4.85.0"
  }
}
